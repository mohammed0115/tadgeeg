# قياس التصنيف — 2026-08-12 · `claude @ d8baeb1` · جهاز تطوير

> **قياس خالص.** التعديل الوحيد في هذه الشحنة سطر لون في قالب (§٤).
> `core/services/pipeline.py` و`apps/**` لم تُمسّ. `dd46332f…` لم يتغيّر.
>
> العيّنة: نفس الـ34 (أول اثنين من كل نوع مخزَّن · `order_by("id")`).
> قراءة-فقط مُثبَتة: `Document` 1853→1853 · `DocumentAnalysisResult` 0→0 ·
> `ExtractedData` 0→0.

---

## 🔴 الحكم — والفرضيتان المطروحتان كلتاهما خاطئة

المهمة عرضت ثلاثة احتمالات. **الثالث هو الصحيح**، والسبب سطر واحد:

```
core/services/normalization.py:145

  "document_type": self._clean_text(payload.get("document_type", "invoice")) or "invoice",
```

**القيمة الافتراضية للنوع هي `"invoice"`** — مكتوبة مرّتين في السطر نفسه
(كافتراضي، وكبديل عند الفراغ). فحين لا يستخرج المحلّل نوعًا — وهو الحال في
17 من 34 — يُكتب «فاتورة».

ثم يقرأ المُصنِّف تلك القيمة في فرعه الأول:

```
core/services/classification/document_classifier.py:139-141

  dtype = structured.get("document_type", "")
  if dtype and dtype in DOCUMENT_TYPES and dtype != "other":
      return {"document_type": dtype, "confidence": 0.90, "method": "structural"}
```

⇒ **المُصنِّف لم يُصنّف شيئًا. أعاد افتراضًا إلى نفسه وختمه بثقة 0.90.**

والـ17 كلها بهذا المسار — لا واحدة منها من الذكاء الاصطناعي ولا من
الكلمات المفتاحية ولا من الخريطة اليدوية:

```
final_method : {'structural': 17, 'default': 14, 'keyword': 3}
final == invoice: 17 — by method: {'structural': 17} — confidence: 0.900 كلها
```

**والخريطة اليدوية بريئة هنا** رغم أنها القائمة التي أشارت إليها المهمة:
`type_map` يغطّي السبعة غير-`other` كلها من `DOCUMENT_TYPES`، فلا قيمة من
المُصنِّف تسقط منها. عيبها في مكان آخر (§١).

**رقم الثقة هو الأخطر:** 0.90 على قيمة لم تُقس. المستودع يعرض افتراضًا
كقياس — نفس النمط الذي أنتج «30 قاعدة» و`Callers:` و`testpaths`.

---

## ١. الجدول الثلاثي

| ما يستطيع المُصنِّف إرجاعه (8) | ما تُخرجه `type_map` (7 مداخل) | `Document.DocumentType` (10) |
|---|---|---|
| `invoice` | → `invoice` | `invoice` ✅ |
| `purchase_order` | → `purchase_order` | `purchase_order` ✅ |
| `bank_statement` | → `bank_statement` | `bank_statement` ✅ |
| `receipt` | → `receipt` | `receipt` ✅ |
| `expense_report` | → `expense_report` | `expense_report` ✅ |
| `payroll` | → **`other`** 🔴 | — لا خيار `payroll` |
| `vat_return` | → `tax_document` | `tax_document` ✅ |
| `other` | → `other` (بالافتراضي) | `other` ✅ |
| — | — | `ledger` 🔴 لا يمكن بلوغه |
| — | — | `contract` 🔴 لا يمكن بلوغه |
| — | — | `financial_statement` 🔴 لا يمكن بلوغه |

**الفجوات — وهي مخرَج §١:**

1. **`payroll` يُكتشف ثم يُرمى.** المُصنِّف يميّزه (ومعه قائمة كلمات كاملة)،
   و`type_map` يحوّله إلى `other`. في العيّنة مستند واحد صُنّف `payroll`
   ويُخزَّن `other`. المعلومة تُنتَج ثم تُتلف.
2. **ثلاثة أنواع في `DocumentType` لا يبلغها هذا المسار أبدًا**: `ledger` ·
   `contract` · `financial_statement`. فعقد لا يمكن أن يُخزَّن عقدًا.
3. **22 قيمة مميّزة موجودة في `Document.document_type` بينما الخيارات 10.**
   `fixed_asset` · `journal_entry` · `quotation` · `sales_order` … تدخل من
   مسار الرفع لا من هذا الأنبوب، ولا واحدة منها خيار صالح.

---

## ٢. مصدر الـ17 — لكل مستند

`method` هو ما يفصل «المُصنِّف أخطأ» عن «الخريطة أخطأت». كلها `structural`:

| النوع المخزَّن | ما أعاده المُصنِّف | الفرع | الثقة | يتطابق؟ |
|---|---|---|---|---|
| bank_statement · cash_voucher · contract · customer_statement · expense_report · general_ledger · goods_receipt_note · ledger · payment_voucher · payroll · proforma_invoice · purchase_invoice · quotation · receipt_voucher · sales_receipt · supplier_statement · tax_declaration | **`invoice`** | `structural` | **0.900** | ❌ ×17 |
| 14 مستندًا آخر | `other` | `default` | 0.000 | جزئيًّا |
| 3 مستندات | `bank_statement` · `payroll` · `receipt` | `keyword` | 1.000 | ✅ ×3 |

**المخزَّن يوافق المُصنِّف في 3 من 34.**

وملاحظتان تنفيان تفسيرين محتملين آخرين:

* `line_items` موجودة في **0 من 34** — ففرع «سطور ⇒ فاتورة» لم يعمل إطلاقًا.
* صفر مستند وصل إلى الفرع الذي يُحسَب ثم يُهمَل (`confidence < 0.85`).

⚠️ **قيد يُصرَّح به:** القياس بـ`use_ai=False`، فمسار الذكاء الاصطناعي
(الخطوة 3 في `classify`) **لم يُختبر** — تشغيله يستهلك OpenAI على 34 مستندًا.
لكنه لا يغيّر النتيجة: الفرع البنيوي يعود بثقة 0.90 **قبل** أن يُنظر إلى
الذكاء الاصطناعي أصلًا. فالذكاء الاصطناعي لا يُستدعى لهذه الـ17 في الإنتاج
أيضًا.

---

## ٣. 🔴 أثر التصنيف على الأحكام — **صفر**

هذا القسم يُبطل فرضيتي في تقرير المرحلة ٥-٠ وفرضية هذه المهمة معًا.

`AuditEngine.evaluate` يمرّ على `self.rule_classes` **بلا أي بوّابة نوع**.
والبوّابة موجودة (`base_rule.py:158 is_applicable`) لكن كل قاعدة تستدعيها
بنفسها — و`extended_rules.py`، الذي يحوي R017 و R018، **لا يستدعيها ولا
يُعرّف `applies_to`**.

القياس محسوب من سجل القواعد لا مكتوبًا:

| مُبوَّبة بالنوع (4) | غير مُبوَّبة (14) |
|---|---|
| R001 · R002 · R004 · R006 | R003 · R005 · R007 · R008 · R009 · **R010** · R011 · R012 · R013 · R014 · R015 · R016 · **R017** · **R018** |

والقواعد الفاشلة فعلًا: `R018 ×29 · R017 ×26 · R003 ×20 · R002 ×14 ·
R005 ×6 · R010 ×6` — **واحدة منها فقط مُبوَّبة (R002)**.

المحاكاة: لو صُنّف كل مستند `other` فسكتت القواعد الأربع المُبوَّبة:

```
تصعيد الآن                 : 29 / 29
تصعيد لو صُنّفت كلها other  : 29 / 29
🔴 يزول بالتصنيف الصحيح     : 0
```

مثال: عقد يفشل الآن في `R002 · R003 · R017 · R018` بدرجة 85/critical.
بتصنيف `other` يسكت R002 وحده، فتصير 75 — **وهي critical أيضًا**، لأن
العتبة 75 و`R017`+`R018` وحدهما 50، و`R003` يضيف 25.

⇒ **الـ29 تصعيدًا ليست أثر سوء التصنيف.** الجذر أن **14 من 18 قاعدة تعمل
على كل نوع مستند**، و`R018` يطلب حقول فاتورة من أي شيء، و`R017` يشترط رمز
ZATCA ما لم تكن العملة **و**الدولة أجنبيتين معًا — والافتراضان `SAR`
و`SA`، فلا يسكت أبدًا.

**التصنيف يبقى عيبًا حقيقيًّا** — لكن أثره على الأحكام يظهر عند الخطوة ٤،
حيث يوجّه V2 قواعدَه بالنوع (`run_audit_compat(document_type=…)`). أما اليوم
فلا يغيّر حكمًا واحدًا.

---

## ٤. عطل الواجهة الأول — أُصلح ومُثبَت في متصفّح

تدرّج `vendor` في `templates/layouts/base_vendor_dashboard.html:26` نسخة من
`blue` في Tailwind، وفيه 50 · 100 · 400 · 500 · 600 · 700 · 900 —
**و200 غائبة بينما `vendor-200` مستعملة 4 مرات** (هذا الملف و
`partials/vendor_topbar.html:16`).

عرّفتُ الدرجة الناقصة (`200: "#bfdbfe"` = `blue-200`) ولم أستبدل استعمالًا،
لأن الاستعمالات صحيحة والتدرّج هو الناقص.

| | قبل | بعد |
|---|---|---|
| `CssSyntaxError` على `/dashboard/files/` | **11** | **0** |
| كل أخطاء console على الصفحة | 11 | **0** |

الصفحة أُعيد فتحها في Chrome بحساب `demo@finai.sa` وفُحصت بصريًّا: شارة
«Organization Context» — وهي العنصر الذي يحمل `text-vendor-700
dark:text-vendor-200` — تُعرض مُنسَّقة، والشريط الجانبي والحالة الفارغة سليمة.

⚠️ **درس قياس:** أول تحقّق أعطى «0 خطأ» وكان كاذبًا مرّتين — الجلسة انتهت
فصُوِّرت صفحة الدخول، ثم القالب كان مُخزَّنًا في ذاكرة خادم يعمل بـ`--noreload`.
الرقم الصحيح ظهر بعد تجديد الجلسة وإعادة تشغيل الخادم.

`<template>` المفقود على `/invoices/upload/` **لم يُلمس** — كامن ويحتاج تشخيصًا.

---

## ٥. ما لم أستطع قياسه

* **مسار الذكاء الاصطناعي في التصنيف** — يستهلك OpenAI. §٢ يبيّن أنه لا
  يُستدعى للـ17 أصلًا، لكن سلوكه على الـ14 التي تسقط إلى `other` غير مقيس.
* **من أين تأتي الـ22 قيمة المخزَّنة** — من مسار الرفع لا من هذا الأنبوب،
  ولم أتتبّعه.
* **أثر التصنيف على V2** — يحتاج تشغيل V2، وهو محجوب بحاجب `invoice_id`.
