# Tadgeeg — الوثيقة الجامعة

> **الموضع:** `docs/MASTER.md` · **التاريخ:** 2026-08-09
> **الحالة:** **مصدر الحقيقة الوحيد.** كل ما سبقها مرجع تاريخي — وعند التعارض،
> هذه الوثيقة تعلو.
> **الأساس المقيس:** `main @ 3d29066` · `claude @ 85e5480` (10 commits · مدفوعة · غير مدموجة)

---

# الجزء الأول — الهدف والمعيار

## ١.١ الهدف النهائي — مُقرّ من صاحب المشروع 2026-08-09

```
نفس المُدخَل  +  عملية أنظف  =  نفس المُخرَج بالضبط
```

**المستخدم لا يلاحظ شيئًا. والمهندس الذي يفتح الشيفرة يرى نظامًا آخر.**

## ١.٢ المرجع الأساس — `main @ 3d29066` (4-8-2026)

**ولماذا 4-8 لا 1-8:** حالة 1-8 (`origin/audit @ 5d02a47`) تحمل عيوبًا معروفة —
تصنيف IAS 7 خاطئ · أرقام مفبركة تُعرض للمدقق كقياسه · سلسلة تدقيق قابلة
للتفرّع · `CTL-006` ينجح دائمًا. **ومطابقة مُخرَجها تعني إعادة تلك العيوب.**

و`3d29066` هو ما يعمل عند العملاء اليوم. فالمرجع «الصحيح الحالي» لا «التاريخي».

### الفرق المسموح — أربعة بنود مُعدَّدة. **لا خامس بلا قرار.**

| # | البند | النوع | الحالة |
|---|---|---|---|
| 1 | `AuditRunResult` — سبعة حقول | تصحيح عيب | ✅ مشحون |
| 2 | دلالة الحجب في V2 | تصحيح عيب | ✅ مشحون |
| 3 | العطل الثالث — مسار V2 | تصحيح عيب · **أحكام تظهر** | ⬜ المرحلة ١ب |
| 4 | نسبة الامتثال بعد الخطوة ٤ | **فرق عرض** | ⬜ الخطوة ٤ · بخطّ فاصل |
| 8 | الاستخراج المُصحَّح يغيّر أحكاماً | تصحيح عيب | ✅ |

**البند 8 تفصيلاً:** في `d8c575e` كان رفع CSV يدقّق حطام استخراج: `vendor_name` احتوى شظية JSON خامة، و`total_amount=0` و`invoice_number='oice'`. بعد تمرير الصف المنظم وإصلاح `subtotal`، اختفت `INV-007` و`DOC-001` و`VAT-002` و`VAT-003` التي كانت تفشل على الحقول المشوّهة، وظهرت `ANO-006` على مبلغ حقيقي. النتيجة المقيسة أعلى: **70.59 مقابل 67.65**. هذا تصحيح عيب موثق، لا فرق غير مفسر في الحكم.

### شرط القبول — أدقّ من «صفر فرق»

> **كل سجل موجود في المرجع يبقى بقيمه حرفيًا. والزيادة سجلات جديدة فقط.**

| نوع الفرق | الحكم |
|---|---|
| مستند كان يُدقَّق ⇒ حكمه اختلف | 🔴 **مرفوض** بلا قرار |
| مستند **لم يكن يُدقَّق** ⇒ صار له حكم | ✅ مقبول — البند 3 |
| رقم معروض تحرّك بلا تغيّر حكم | ⚠️ بند مُعدَّد + إعلان (البند 4) |

**البند 4 تفصيلًا:** مسار المستندات ينتقل من 18 قاعدة إلى 131 ⇒ المقام في
`re_compliance` (`page_views.py:4355`) يتضاعف ×7 ⇒ **الرقم المعروض يتغيّر بلا
أن تتغيّر جودة مستند واحد.** يُعالَج بخطّ فاصل عند تاريخ النشر أو تصفية
بـ`engine_version` — ولا يُشحن بلا أحدهما.

## ١.٢ب المُخرَج يشمل العرض لا الحكم وحده

> **صفر فرق في الحكم · وصفر فرق في العرض غير مُعلَن.**

المستخدم لا يرى الحكم — يرى اللوحة والعدّادات والرسوم والمسارات.
التفاصيل في `docs/FRONTEND_CONTRACT.md`.

## ١.٢ج 🔴 قاعدة الأولوية عند التعارض

> **المُخرَج المطابق يقدّم على البنية دائمًا.**
>
> منصّة تدقيق تُحاسَب على أحكامها لا على جمال شيفرتها. **وأي تنظيف يهدّد حكمًا
> يُلغى — لا يُخفَّف ولا يُؤجَّل.**

## ١.٣ الغرض العملي

النظام يُعرَض على مهندسين خارجيين يحكمون على: **SOLID · تعدد المستأجرين ·
الأمن · الصلابة.** فالمعيار ليس «هل النظام سليم» بل **«هل يستطيع المراجع أن
يُثبت أنه سليم في ساعة»**.

---

# الجزء الثاني — الواقع المقيس

## ٢.١ خمس منظومات حكم حيّة

| المنظومة | المسار | القواعد | يعمل على |
|---|---|---|---|
| المدقق القديم | `core/services/invoice_validator.py` | 34 | الفواتير — متزامن |
| المحرك النشط **V1** | `apps/rule_engine/executors/audit_pipeline.py` | 131 | الفواتير · API · GAAP |
| المحرك النشط **V2** | `apps/rule_engine/pipeline/v2/pipeline.py` | 131 | 11 نوع مستند مُوقَّع |
| تحليلات القيود | `apps/audit/services/journal_analytics.py` | 8 `JA-*` | دفتر الأستاذ |
| `AuditEngine` القديم | `apps/audit/audit_engine.py` | 18 `R001-R018` | مسار المستندات |
| *الكتالوج* | `apps/rule_engine/catalog/document_rules.py` | *236 مبذورة نحو نائب* | — |

**المجموع الحيّ: 191 قاعدة.** والرقم المعلن في `README.md`: **30**.

## ٢.٢ أربعة مخططات ترجيح

| المنظومة | الترجيح | المستويات |
|---|---|---|
| المدقق القديم | **لا ترجيح** — `ناجحة/34 × 100` | 4 محسوبة · تُهمَل |
| `merge_risk_assessment` ← **الكاتب الفعلي** | `100 − validation_score` · قيعان 78/72/58 | **3** — لا `critical` |
| `RiskEngine` (V1+V2) | 5 مكوّنات 0.40/0.20/0.20/0.10/0.10 | 4 — ≥75/50/25 |
| `AuditEngine` | CRITICAL 40 · HIGH 25 · MEDIUM 10 · LOW 5 | — |

🔴 **المقياسان معاكسان:** المرتفع **جيّد** في المدقق القديم، **سيّئ** في المحرك.
درجة 90 = فاتورة سليمة هنا، حرجة هناك.

## ٢.٣ التصحيحات الثلاثة التي غيّرت مُخرَجًا — بقرار

| # | التصحيح | الأثر |
|---|---|---|
| 1 | **IAS 7** — خمسة عيوب | `credit note` و`audit` لم تعدّا استثماريتين · `purchase` لم تعد رأسمالية |
| 2 | **دلالة الحجب في V2** | الحجب يعود منسوبًا إلى قاعدة · لا حجب بالمجموع |
| 3 | **عطل `run_audit_compat`** | 11 نوع مستند تُدقَّق فعلًا بعد أن كانت لا تُدقَّق |

⚠️ **البند ١ يعني أن مستندات دُقّقت قبل 4 أغسطس قد تحمل تصنيفًا نقديًا خاطئًا.**
قرار إعادة تدقيقها لم يُفتَح بعد.

## ٢.٤ التقييم على المحاور الأربعة

| المحور | التقدير | الدليل |
|---|---|---|
| **الأمن** | 🟢 8/10 | ترويسات كاملة مربوطة بـ`DEBUG` · MFA · سلسلة مُهشَّرة · SSRF |
| **الفساد والتلاعب** | 🟢 9/10 | قيد تفرّد ⇒ التفرّع مستحيل بالبناء · أعمدة مجمّدة · `ChainCheckpoint` |
| **الأعطال** | 🟢 8/10 | ثلاث درجات AI · قاطع دائرة · `on_commit` · `NOT_APPLICABLE` مفصولة عن `INSUFFICIENT_DATA` |
| **تعدد المستأجرين** | 🟡 6/10 | 38 نموذجًا · **427 تصفية يدوية** · لا مدير مقيَّد · 24 اختبار عزل |
| **التغيير (SOLID)** | 🟡 5/10 | `RuleRegistry` سليم · 12 صنفًا مجردًا · **مقابل** 5 منظومات و`page_views` 5,869 |
| **الحمل** | 🔴 **لا تقدير** | **اختبار الحمل مكتوب ولم يُشغَّل قط** |

## ٢.٥ الأعناق المعروفة

1. **المعالجة متزامنة على مسار الفواتير** — `ValidationPipelineService` داخل الطلب
2. **~12 استعلامًا لكل فاتورة** · ZIP بـ500 ⇒ ~6,000 · **ثم محرّك ثانٍ**
3. **645 استعلامًا** في 70 وحدة عرض
4. OCR + GPT-4o لكل مستند — عنق خارجي
5. MySQL واحد بلا نسخ قراءة

**التقدير المعماري (لا قياس):** عشرات ✅ · مئات 🟡 · آلاف 🔴

## ٢.٦ الدَّين المتبقّي بالأرقام

| البند | الرقم |
|---|---|
| `page_views.py` | 5,869 سطرًا · أطول دالة 486 |
| `DocumentReportService` | 1,469 سطرًا · 36 دالة |
| قواعد الفواتير في دالة واحدة | 410 أسطر |
| تجريدات بلا مستدعٍ إنتاجي | 3 |
| Django | 4.2.30 — **خارج الدعم منذ أبريل 2026** |
| حزم خارج ملف القفل | **10** منها `pyotp` و`mysqlclient` |
| تصادمات رموز | `CTL-004`/`CTL-04` · `CTL-005`/`CTL-05` · `ANO-01` |
| `Docs/` و`docs/` | يختلفان بحالة حرف ⇒ لا يُستنسخ على macOS/Windows |
| عتبة التغطية | 45% |

---

# الجزء الثالث — ما أُنجز

## ٣.١ عطلان إنتاجيان مُصلَحان

**١. `run_audit_compat` — `RecursionError`.** `quota_gate` يُرقّع الوحدة ثم
يعيد استيرادها وقت النداء فيحصل على نفسه. 11 نوع مستند لا تُدقَّق. **مرّ تحت
3,936 اختبارًا** لأن الاختبارات تستدعي المُغلَّف لا الغلاف.

**٢. `AuditRunResult` ينقصه سبعة حقول.** `tasks.py:139` يقرأ `failed_count`
غير المعرَّف ⇒ `AttributeError` بعد نجاح التدقيق ⇒ **المهمة تُبلّغ عن فشل لكل
مستند دقّقته بنجاح.**

## ٣.٢ خمسة حرّاس دائمة

| الحارس | يمنع |
|---|---|
| `test_adapter_contract.py` | عقد الجسر — يستخرج الحقول بالـAST من الشيفرة |
| `test_engine_callers.py` | سقف مستدعي المحرّكات — يتناقص ولا يصعد |
| `test_quota_gate_wrapper.py` | ارتداد الغلاف |
| `test_v2_blocking_semantics.py` | حجب بلا قاعدة منسوبة |
| ميزانيات الاستعلامات · الاستثناءات الصامتة | تراجع الأداء والصمت |

**كلٌّ منها مُثبَت بإعادة إنتاج العيب** — لا بمخالف مصطنع.

## ٣.٣ سجل الانحرافات — 49 بندًا

`docs/EXECUTION_TRACKER.md` §15. **قرابة الثلث أخطاء المشرف.**

**وهذا أقوى ورقة في المراجعة:** مراجع خبير لا يعاقب على الدَّين، يعاقب على
الدَّين **غير المعروف**. نظامان بنفس العيوب — أحدهما يوثّقها بأرقام، والآخر
يصمت: الأول «مُدار»، والثاني «خارج السيطرة».

---

# الجزء الرابع — خطة الشهر

**الإضافات مؤجَّلة بقرار ⇒ `main` جامد ⇒ لا تعارضات دمج.**

## الأسبوع ١ — القياس والأمن

| # | العمل | ملاحظة |
|---|---|---|
| 1 | 🔴 **شغّل اختبار الحمل** | `deployment/loadtest/` · **ساعة** · يقرّر شكل الشهر |
| 2 | **Django 5.2 + ملف القفل** | حاجز واحد: نمط `STORAGES` من `settings/test.py:21` |
| 3 | **المرجع الذهبي** + إثبات أنه يفشل | الأداة التي تُثبت «صفر فرق» |
| 4 | دمج الـPR | يُغلق التصحيحات |

> **البند ١ أولًا.** إن أظهر سعة أقل مما نظن، الأسبوع الثالث يتحوّل من SOLID
> إلى أداء — ويُفضَّل معرفة ذلك في اليوم الأول لا الثلاثين.

## الأسبوع ٢ — تعدد المستأجرين مُبرهَنًا

| # | العمل |
|---|---|
| 1 | `OrganizationScopedManager` على الـ38 نموذجًا — العزل افتراضي لا اختياري |
| 2 | **اختبار عزل مُولَّد** — يمسح كل نموذج بالـAST · مستأجران · (أ) لا يرى (ب) |
| 3 | فهارس مركّبة `(organization, …)` على القراءات الساخنة |

**البند ٢ يقلب جواب المراجع** من «انضباط في 427 موضعًا» إلى **«اختبار يفشل»**.

## الأسبوع ٣ — SOLID حيث يُرى · كله تحت المعيار

| # | العمل | الأثر |
|---|---|---|
| 1 | **الخطوة ٤ + ٦ب** — مسار واحد لكل مستند | 5 → **3** منظومات · **ونصف الحمل يسقط** |
| 2 | حذف `AuditEngine` | 18 قاعدة و~1,000 سطر |
| 3 | توصيل `providers.py` (3 مواقع سردية) | صفر تجريد معطّل |
| 4 | تفكيك مجموعة `DUP` بـ`AuditContext` | برهان أن النمط يُعمَّم |
| 5 | حسم `CTL-004`/`CTL-04` · `CTL-005`/`CTL-05` | مفتاح هوية غامض |

**البند ١ ليس تنظيفًا — إنه مضاعفة سعة.** كل مستند يُدقَّق مرتين اليوم.

## الأسبوع ٤ — العرض · **موعد لا مساحة، يبدأ اليوم ٢٢**

| # | العمل |
|---|---|
| 1 | **`ARCHITECTURE.md`** — القرارات وأسبابها · الدَّين بالأرقام · آليات منع التراجع |
| 2 | **`README.md` صادق** — 191 قاعدة · MySQL · العتبات الحقيقية |
| 3 | رقم السعة منشورًا مع أعناقه |
| 4 | `EXECUTION_TRACKER` §15 معروضًا لا مخفيًّا |
| 5 | `git mv Docs Documentation` — 13 مرجعًا نصّيًا · Linux حصرًا |

> ⚠️ **أرجح مخاطر الشهر:** أن ينزلق العمل التقني فيأكل هذا الأسبوع — وهو
> الأسبوع الوحيد الذي يقرأه المراجع.

## ما يُترك بقرار — يُعلَن ولا يُخفى

| البند | السبب |
|---|---|
| `page_views.py` | أسبوعان وحده · موثّق بخطة |
| الـ191 قاعدة كلها | شهور · والنمط مُبرهَن بمجموعة `DUP` |
| كتاب القواعد · برنامج البيانات | لا يخدمان المراجعة |
| CSP `unsafe-eval` | هجرة قوالب · موثَّق بتبرير |
| إعادة تدقيق مستندات ما قبل 4 أغسطس | قرار مهني مفتوح |

---

# الجزء الخامس — مقاييس النجاح

| المحور | اليوم | بعد شهر |
|---|---|---|
| السعة | **غير مقيسة** | رقم منشور بأعناقه |
| الأمن | 🟡 Django خارج الدعم | 🟢 مدعوم · القفل كامل |
| المستأجرون | 🟡 427 تصفية يدوية | 🟢 مدير + اختبار مُولَّد |
| منظومات الحكم | 5 | **3** |
| مخططات الترجيح | 4 | 3 |
| المعمارية موثَّقة | ❌ | ✅ |
| تجريدات معطّلة | 3 | 2 |

---

# الجزء السادس — قواعد التشغيل

## ٦.١ العقد

`CLAUDE.md` في الجذر. المحظورات · القاعدة الحاكمة: **اختبار فاشل يُبلَّغ عنه لا
يُصلَح** · شكل التقرير التسعة أقسام.

## ٦.٢ خمس قواعد مستخلَصة من العمل — لا من كتاب

1. **الوثيقة لا تفشل، الاختبار يفشل.** كل مرحلة تنتهي بحارس لا بعلامة ✅.
2. **حارس لم يُر وهو يفشل ليس حارسًا** — ويُثبَت **بإعادة إنتاج العيب** لا
   بمخالف مصطنع. (ثلاثة حرّاس وُلدوا عاجزين عن الفشل في هذا المستودع.)
3. **الرقم بلا مسار ليس رقمًا.** لا قائمة يدوية لما يمكن حسابه — قائمة
   `Callers:` مكتوبة بيد حملت واحدًا من أربعة، فأفلت مستدعٍ من الترحيل.
4. **حين تتعارض الشيفرة مع قياس، الشيفرة أقرب** حتى يُثبَت أن الأداة سليمة.
   القياس يحمل أداةً قد تكذب.
5. **أنهِ أو احذف. لا تترك اثنين.** ومن يبني بديلًا يحذف المُستبدَل في العمل
   نفسه — وإلا فالعمل **غير مكتمل**، لا «مكتمل مع دَين».

## ٦.٣ الجذر الذي أنتج كل ما وجدناه

> **وحدة الحقيقة في هذا المشروع كانت قائمةً تُصان باليد.**

`testpaths` · `requirements.lock.txt` · `REGISTERED_RULES` · `INSTALLED_APPS` ·
قائمة `Callers:` · «30 قاعدة» · قائمة النماذج في المُتحقِّق الليلي ·
استثناءات الحارس. **كل واحدة قائمة، وكل واحدة كان يمكن حسابها.**

---

# الجزء السابع — الأرشيف

الوثائق التالية **مرجع تاريخي**. عند التعارض، `MASTER.md` تعلو:

| الملف | ما يبقى نافعًا فيه |
|---|---|
| `RULE_INVENTORY.md` | جرد الـ191 والثلاثين بندًا · المسارات الدقيقة |
| `EXECUTION_TRACKER.md` | **§1 الثوابت المقدّسة · §15 الانحرافات الـ49** ← يبقى حيًّا |
| `UNIFICATION_PLAN.md` | تفصيل المخاطر R1–R11 · بوابات التوقّف |
| `UNIFICATION_STEPS_4_9.md` | الديفّات المُجهَّزة · استعلامات المراقبة |
| `GOLDEN_MASTER_SPEC.md` | مواصفة الأداة — **تُنفَّذ في الأسبوع ١** |
| `EXTERNAL_REVIEW_READINESS.md` | أسئلة المراجع المتوقّعة وأجوبتها |
| `CTO_DECISION_shipment_1..7.md` | سياق كل قرار وسببه |

## ٧.١ تناقضات معروفة في الأرشيف — صُحّحت هنا

| التناقض | الحكم النهائي |
|---|---|
| بوابة الخطوة ٤ عُدّلت مرتين (استعلام إنتاج → `Dataset/`) | **المرجع الذهبي** هو البوابة |
| حذف `AuditEngine` بعد الخطوة ٤ | **بعد ٦ب** — `processor.py:215` مخرج طوارئ للفواتير |
| «V2 ≥ V1 دائمًا» | **خطأ** — المُعدِّلات أحادية الاتجاه، والأساسان مختلفان |
| قياس الشحنة ٤ (400 مستند · صفر فارق) | **باطل** — الأداة مرّرت `Document.id` |
| الأصداف = عيب استيعاب عاجل | **خطأ** — 2 من 1,853 · أولوية منخفضة |
| ثلاثة أرقام تقدّم (0% · 50% · 25%) | **المقياس = المخرَج البنيوي** ⇒ صفر حتى تسقط منظومة |
| `re_audit_runs` · `documents_purchaseorder` | **أسماء مخترَعة** — الصحيح `re_audit_run` · `purchase_orders` |

---

**الخطوة التالية:** تشغيل اختبار الحمل. ساعة، ويقرّر شكل الشهر.
