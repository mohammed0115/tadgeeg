"""AI providers and learned models.

`providers.py` decides where narrative text comes from and guarantees a floor.
`false_positive_model.py` is the part that actually learns — from your own
auditors' verdicts, not from a general-purpose model.
"""
