# فحص محلّي على `main` — قبل النشر

> **النطاق:** جهاز تطوير · `runserver` محلّي · **صفر تعديل شيفرة أو قالب** ·
> **صفر commit وصفر push على `main`**. هذا التقرير مُلتزَم على `claude`.
> التصريح: رفع مؤقّت لبند «لا لمس main» لغرض `checkout` و`pull --ff-only` فقط.

| البند | القيمة |
|---|---|
| رأس `main` بعد `pull --ff-only` | **`eefa064`** — لا `26e7d86` كما توقّعت المهمة |
| الفارق | commit **واحد** بعد `26e7d86`، و**وثائقي بحت** (`docs: audit deployment readiness`) |
| تقدُّم محلّي | `main` كان متأخّرًا **54** commit (لا 53) |
| شجرة العمل | `?? docs/PLAN.md` فقط — غير متتبَّع، وغير موجود على `main`، فلم يُهجَر شيء |
| نسخة احتياطية | `db_runtime.sqlite3` (43,130,880 بايت) قبل أي `migrate` |
| الحساب المستعمل | `demo@finai.sa` — `is_staff=False` · `is_superuser=False` · `role=senior_auditor` · اشتراك المنظمة `active` |

**لا رقم منقول من تقرير سابق.** أُعيد قياس كل شيء.

⚠️ الفارق بين `eefa064` و`26e7d86` وثائقي، فالشيفرة المفحوصة **هي** شيفرة
`26e7d86`. تحقّقت بـ`git diff --stat 26e7d86..HEAD -- . ':(exclude)docs'` ⇒ فارغ.

---

## ١. الهجرات — على `main`

```
$ python manage.py migrate
Running migrations:
  No migrations to apply.

$ python manage.py showmigrations | grep -c "\[ \]"
0

$ python manage.py makemigrations --check --dry-run
No changes detected
```

| # | السؤال | المقاس |
|---:|---|---|
| ١ | هجرات طُبِّقت الآن | **صفر** — المجموعة كانت محدَّثة سلفًا |
| ٢ | غير مُطبَّقة بعدها | **صفر** ✅ |
| ٣ | `No changes detected` | **نعم** ✅ |

سبب الصفر في البند ١: مجموعة الهجرات على `main` و`claude` **متطابقة ملفًا بملف**
(`comm` على شجرتي الفرعين ⇒ لا فرق في أي اتجاه)، و`0017`/`0018` كانتا طُبِّقتا
في جلسة سابقة. فلا انحراف — القاعدة كانت متوافقة مع `main` قبل الانتقال إليه.

---

## ٢. `libzbar0` — 🔴 لم يُثبَّت

```
$ sudo -n true            → sudo: a password is required
$ sudo -n apt-get install -y libzbar0
sudo: a password is required
```

| # | السؤال | المقاس |
|---:|---|---|
| ٤ | التثبيت نجح | **لا** — `sudo` يطلب كلمة مرور والجلسة غير تفاعلية |

**ولم أُجرّب بديلًا واحدًا**، عملًا بالبند. وتشخيص قراءة فقط يُثبت أن الغياب
حقيقي لا مسألة مسار تحميل:

```
ldconfig -p | grep zbar            → not in cache
/usr/lib/x86_64-linux-gnu/libzbar* → not present
dpkg -l | grep zbar                → no package
python -c "import pyzbar.pyzbar"   → ImportError: Unable to find zbar shared library
```

**البند يبقى مفتوحًا.**

---

## ٣. التشغيل والفحص

### الإعداد

```
$ python manage.py collectstatic --noinput
0 static files copied to '/home/mohamed/tadgeeg/staticfiles', 434 unmodified.

$ DEBUG=False python manage.py shell -c "…"
DEBUG: False
ALLOWED_HOSTS: ['localhost', '127.0.0.1', 'testserver', 'test.tadgeeg.com', '69.62.115.97']

$ DJANGO_DEBUG=False python manage.py shell -c "…"
DEBUG: True          ← تحذير المهمة مؤكَّد بالقياس
```

الخادم: `DEBUG=False python manage.py runserver 0.0.0.0:8000 --noreload` ·
`GET / → HTTP 200`.

⚠️ `--noreload` مقصود: عامل واحد، فعدّ الطلبات لا يختلط بإعادة تحميل.

### أ · FI-01 على `main`

```
pre-click DOM: [{'text': 'Upload 1', 'type': 'button', 'disabled': False,
                 'inForm': False, 'xOn': ['@click=upload'], 'visible': True}]
>>> SINGLE CLICK <<<
   POST /auditor/upload/ count = 1
     [1] 11:11:45.822
   11:12:10.731  POST -> 302
```

| # | السؤال | المطلوب | المقاس |
|---:|---|---|---|
| ٥ | عدد `POST /auditor/upload/` | 1 | **١** ✅ |
| ٦ | صفوف `Invoice` المُنشأة | 1 | **١** ✅ |

🔴 **FI-01 مُغلَق على `main`.** وللمقارنة على الجهاز نفسه وبالمسار نفسه:
`fi01_probe_singleclick.pdf` قبل الإصلاح ⇒ **صفّان**؛ و`main_verify_fi01.pdf`
بعده ⇒ **صفّ واحد**.

وشاهد جانبي متّسق: خطأ `cloneNode` كان يقع **مرّتين** قبل الإصلاح ويقع
**مرّة** الآن — أي أن Alpine تُنفَّذ مرّة واحدة.

### ب · القوالب الستّ

جولة قراءة فقط، بلا ضغط وبلا إنشاء أي صف.

| القالب | المسار | وسوم Alpine | النداءات المضاعفة |
|---|---|---:|---|
| `zatca/dashboard` | `/compliance/zatca/` | 2 | `x4 GET /api/v1/notifications/unread-count/` · `x4 GET /api/v1/zatca/dashboard/` |
| `invoices/list` | `/invoices/` | 2 | `x4 GET /api/v1/notifications/unread-count/` |
| `banking/dashboard` | `/banking/` | 2 | `x4 GET /api/v1/notifications/unread-count/` |
| `alerts/index` | `/audit/alerts/` | 2 | `x4 GET /api/v1/notifications/unread-count/` |
| `reports/index` | `/reports/` | 2 | `x4 GET /api/v1/notifications/unread-count/` |
| `audit/rule_builder` | `/audit/rule-builder/` | 2 | `x4` على `unread-count` و`dsl-schema/` و`rule-builder/` |

| # | السؤال | المقاس |
|---:|---|---|
| ٧ | قوالب تُصدر نداءات مضاعفة | **٦ / ٦** |
| ٨ | 🔴 نداء **كتابة** مضاعف | **صفر** ✅ |

🔴 **والثامن هو الفاصل، وهو صفر.** كل نداء مضاعف على الستّة هو `GET`. لا
`POST` ولا `PUT` ولا `DELETE` مضاعف. ⇒ **بطء وحِمل، لا ازدواج بيانات.**
الإصلاح الجزئي (صفحتا الرفع فقط) عالج كل ما يكتب.

### ج · QR بعد `libzbar0`

| # | السؤال | المقاس |
|---:|---|---|
| ٩ | `has_qr_code = 1` | **غير قابل للقياس** — `libzbar0` لم يُثبَّت (البند ٤) |
| ١٠ | `DOC-004` · `VAT-005` تقيسان الآن؟ | **لا** |

الحالة القائمة على القاعدة: `has_qr_code = 1` في **صفر** من **79** فاتورة،
و`qr_code_valid = 1` في **صفر**. ⇒ القاعدتان تفشلان على كل فاتورة بلا استثناء،
ولا يمكن لأي مستند أن يُنجحهما في هذه البيئة.

⚠️ ورسالة السجل مُضلِّلة: `core/services/qr_scanner.py:97` يقول
«pyzbar not installed — Run: pip install pyzbar»، **و`pyzbar` مثبَّتة بالفعل**؛
الناقص حزمة النظام `libzbar0`. فاتّباع النصيحة المسجَّلة لا يُصلح شيئًا.

### د · الصفحات الحرجة

| الصفحة | HTTP | نصّ الجسم | وسوم Alpine | `x-data` مُهيَّأة | أخطاء |
|---|---:|---:|---:|---|---:|
| `/dashboard/` | 200 | 2,751 | 1 | 3 / 3 | 0 |
| `/invoices/<pk>/` | 200 | 6,298 | 1 | 6 / 6 | 0 |
| `/reports/invoice-audit/` | 200 | 8,434 | 1 | 30 / 30 | 0 |
| `/invoices/` | 200 | 1,165 | **2** | 3 / 3 | 0 |

| # | السؤال | المطلوب | المقاس |
|---:|---|---|---|
| ١١ | صفحة لا تُعرض | صفر | **صفر** ✅ |
| ١٢ | أخطاء console على الصفحات الحرجة | صفر | **صفر** ✅ |
| ١٣ | Alpine يتفاعل | نعم | **نعم** — `reactive: open false->true` |

⚠️ **تصحيح ذاتي:** في أول جولة سجّلت `/invoices/<pk>/` بـ**404** وعددتها
«صفحة لا تُعرض». وكان الخطأ خطئي: مرّرت UUID بلا شرطات. وبالصيغة الصحيحة
تُعرض 200 بـ6,298 حرفًا وستّ مكوّنات مُهيَّأة وصفر موارد فاشلة. **العدد ١١ صفر
لا واحد.**

#### 🔴 لكن صفحتين خارج القائمة الحرجة تُخرجان أخطاء JS

| الصفحة | أخطاء | العيّنة | استجابات فاشلة |
|---|---:|---|---|
| `/audit/rule-builder/` | **28** | `Cannot read properties of null (reading 'description' / 'expression_dsl' / 'name')` | **لا شيء** |
| `/audit/alerts/` | **14** | `Cannot read properties of null (reading 'channels' / 'cooldown_minutes' / 'id')` | **لا شيء** |

ليست أخطاء صلاحيات ولا شبكة — صفر استجابة ≥400. هي إسناد إلى `null` في
القالب: كائن مُختار غير موجود بينما الربط يقرأ حقوله. **وعددها زوجي لأن
Alpine مُكرَّرة**: 28 = 2×14 و14 = 2×7. فإصلاح التكرار يُنصّفها ولا يُزيلها.
**لم أشخّصها، وهي بند مسجَّل.**

---

## ٤. التنظيف — 🔴 لم يُنفَّذ · ينتظر قرارك

| # | السؤال | الحالة |
|---:|---|---|
| ١٤ | حالة القاعدة بعد التنظيف | **معلَّق — لم أُسقِط القاعدة** |

البند نصّ: «أسقِط قاعدة القياس وأعِد migrations». ولا توجد **قاعدة قياس
منفصلة**: القياس جرى على `db_runtime.sqlite3` نفسها، لأن الشحنات السابقة
طلبت القياس على قاعدة التشغيل.

وميزان الفعل:

| ما سيُحذف | العدد |
|---|---:|
| فواتير | **79** |
| مستندات | **2,867** |
| جلسات تدقيق | **54** |
| منظمات | **13** |
| كل حسابات المستخدمين | الكل — بما فيها حسابك الذي تتصفّح به |

| ما سيُزال فعلًا من أثري | العدد |
|---|---:|
| `fi01_probe_singleclick.pdf` | 2 |
| `fi01_causation_deduped.pdf` | 2 |
| `fi01_verify_fix.pdf` | 1 |
| `main_verify_fi01.pdf` | 1 |
| **المجموع** | **6 من 79** |

⇒ **إسقاط القاعدة يُتلف 79 صفًّا و2,867 مستندًا و13 منظمة وكل الحسابات، لإزالة
ستّة صفوف.** وهو غير قابل للتراجع عمليًّا (النسخة الاحتياطية تُعيد البيانات
لكنها تُعيد معها الصفوف الستّة). فتوقّفت وأسأل بدل أن أُرجّح.

النسخة الاحتياطية محفوظة:
`scratchpad/db_runtime.pre_main_check.sqlite3` (43,130,880 بايت).

ولم أحذف صفًّا واحدًا — السلسلة مُهشَّرة، والحذف صفًّا صفًّا منهيٌّ عنه بالبند.

---

## ٥. 🔴 الحكم

> ## نعم — `main` جاهز للنشر على **dev**، بثلاثة بنود مسجَّلة.
>
> **ولا يُنشَر إلى الإنتاج بهذا الفحص.**

**ما يسنده:**

- FI-01 مُغلَق ومُتحقَّق منه على `main` نفسه: ضغطة ⇒ طلب ⇒ صفّ (٥، ٦).
- الهجرات نظيفة ولا انحراف نماذج (١، ٢، ٣).
- الصفحات الحرجة الأربع تُعرض بلا خطأ، وAlpine حيّ (١١، ١٢، ١٣).
- **لا نداء كتابة مضاعفًا في المنتج كلّه** (٨) — فلا طريق معروف لازدواج بيانات.

**البنود الثلاثة المسجَّلة — ولا يحجب أيّها dev:**

1. **`libzbar0` غير مثبَّت** ⇒ `DOC-004` و`VAT-005` تفشلان على 79 من 79.
   يجب التحقّق من وجود الحزمة في صورة dev، وإلا انتقل العيب معها.
2. **ستّة قوالب تُضاعف نداءات القراءة** — حِمل مضاعف على `unread-count`
   و`zatca/dashboard` و`rule-builder`. أداء لا سلامة.
3. **`rule-builder` و`alerts` تُخرجان 28 و14 خطأ JS** لم تُشخَّص.

**ولماذا لا يُنشَر إلى الإنتاج:** البنود المفتوحة من الشحنات السابقة لم
تُعالَج — `VAT-001` تجتاز على افتراض النموذج، والعملة تُختم `SAR` بلا دليل،
والمستخلص الاحتياطي يكتب قيمًا واثقة خاطئة. وكلّها **أحكام تدقيق تُعرض
للمدقّق كدليل**، وهي أخطر من أي بند أعلاه.

---

## ٦. ما لم أفعله

- **صفر commit وصفر push على `main`.** بقي `main` عند `eefa064` كما سحبته.
- صفر تعديل على أي ملف شيفرة أو قالب، على أي فرع.
- لم أُسقِط قاعدة ولم أحذف صفًّا (§٤).
- لم أُجرّب بديلًا لـ`libzbar0`.
- لم أنشر إلى dev ولا استعملت Docker — `runserver` محلّي فقط.
- لم أشخّص أخطاء `rule-builder`/`alerts`.

---

## ٧. أخطاء التوجيهات — كما طُلب

| ورد | المقاس |
|---|---|
| «سبع migrations على جداول التدقيق» | **اثنتان**: `0017_invoice_audit_document` و`0018_..._backfill` |
| «فحصتُ `deploy_finish.sh` وهو ليس مسار النشر» | مسجَّل. ولم أفحص أي مسار نشر في هذه الشحنة — `runserver` محلّي فقط بأمر المهمة |
| «`main` متأخّر 53 commit» | **54** |
| رأس `main` المتوقَّع `26e7d86` | **`eefa064`** — وبينهما commit وثائقي واحد لا يمسّ الشيفرة |
| «بنمط VAT-002 نفسه» (شحنة سابقة) | `VAT-002` لا تمتنع؛ فرع `else` يجتاز على العدم |
| `models.py:75` · `invoice_validator.py:303-307` (شحنة سابقة) | الصحيح `:83` و`302-313` |

---

## ٨. الانحرافات — وأخطائي أوّلًا

1. **🔴 سجّلت `/invoices/<pk>/` بـ404 ثم تبيّن أنه خطئي** — UUID بلا شرطات.
   بالصيغة الصحيحة 200. صحّحت العدد ١١ من واحد إلى صفر.
2. أنشأ فحصي **6 فواتير** في قاعدة التطوير عبر الجلسات، منها **2 اليوم**
   (`fi01_verify_fix.pdf` و`main_verify_fi01.pdf`). مُفصح عنها بالاسم.
3. أول حسابين جرّبتهما (`qa.temp.user2@example.com` ثم غيره) رُدّا إلى
   `/billing/plans/` — منظماتهما بلا اشتراك فعّال. فاخترت `demo@finai.sa`
   بعد قياس حالة الاشتراكات، لا بالحدس. والمسبار توقّف قبل أي ضغطة في
   الحالتين، فلم يُفسد قياسًا.
4. **رأس `main` ليس `26e7d86`** بل `eefa064`؛ والتأخّر 54 لا 53.
5. `main` المحلّي كان متأخّرًا فعلًا — ولو فُحص على `claude` لفُحص شيء قريب
   لا الشيء نفسه، كما نصّ التصريح. (وقد تبيّن أن الشيفرة متطابقة، لكن ذلك
   **نتيجة قياس** لا افتراض مسبق.)
6. عدد الفواتير تغيّر أثناء الجلسات (69 ← 71 ← 75 ← 77 ← 79)، فلم أنقل رقمًا.
7. لم أقس البند ٩ ولا ١٠ — محجوبان بالبند ٤.

---

## ٩. الأرقام الأربعة عشر

| # | البند | القيمة |
|---:|---|---|
| ١ | هجرات طُبِّقت الآن | صفر (المجموعة محدَّثة سلفًا) |
| ٢ | غير مُطبَّقة بعدها | **صفر** ✅ |
| ٣ | `No changes detected` | **نعم** ✅ |
| ٤ | `libzbar0` ثُبِّت | **لا** — `sudo` تفاعليّ |
| ٥ | `POST /auditor/upload/` من ضغطة | **١** ✅ |
| ٦ | صفوف `Invoice` | **١** ✅ |
| ٧ | قوالب بنداءات مضاعفة | **٦ / ٦** |
| ٨ | 🔴 نداءات كتابة مضاعفة | **صفر** ✅ |
| ٩ | `has_qr_code = 1` | غير قابل للقياس (٤) |
| ١٠ | `DOC-004`/`VAT-005` تقيسان | **لا** |
| ١١ | صفحات لا تُعرض | **صفر** ✅ |
| ١٢ | أخطاء console على الحرجة | **صفر** ✅ |
| ١٣ | Alpine يتفاعل | **نعم** |
| ١٤ | القاعدة بعد التنظيف | **معلَّق — ينتظر قرارك** |

---

## ١٠. ما يحتاج قرارًا

1. **🔴 التنظيف (§٤).** إسقاط القاعدة يُتلف 79 فاتورة و2,867 مستندًا و13 منظمة
   وكل الحسابات، لإزالة 6 صفوف. أأُسقِطها، أم أترك الصفوف الستّة معلَّمة
   بالاسم، أم تريد مسارًا ثالثًا؟
2. **`libzbar0`** — يحتاج `sudo` بكلمة مرور. أتُثبّتها أنت، أم تمنحني وسيلة
   غير تفاعلية؟ ودون ذلك يبقى البندان ٩ و١٠ غير مقيسين، وتبقى `DOC-004`
   و`VAT-005` فاشلتين على كل فاتورة في dev أيضًا إن غابت الحزمة عن الصورة.
3. **القوالب الستّ** — شحنة إصلاح، أم بند مسجَّل؟ لا تكتب شيئًا (٨ = صفر)،
   لكنها تُضاعف الحِمل.
4. **`rule-builder` (28 خطأ) و`alerts` (14)** — أأفتح شحنة تشخيص؟
