# خطة معالجة النظام — الكاملة

> `docs/PLAN.md` · 2026-08-11
> **تحلّ محلّ:** `PLAN_UNHURRIED.md` · `UNIFICATION_PLAN.md` · `UNIFICATION_STEPS_4_9.md`
> **تُقرأ مع:** `MASTER.md` §١.٢ (المعيار) · `FRONTEND_CONTRACT.md` · `EXECUTION_TRACKER.md` §15

---

# الجزء الأول — الأساس

## ١.١ الهدف

```
نفس المُدخَل  +  عملية أنظف  =  نفس المُخرَج بالضبط
```

**المستخدم لا يلاحظ شيئًا. والمهندس الذي يفتح الشيفرة يرى نظامًا آخر.**

**الغرض:** مهندسون خارجيون سيفحصون البيئة الداخلية ويحكمون على SOLID ·
تعدد المستأجرين · الأمن · الصلابة. **الحكم على البنية لا على الوظيفة.**

## ١.٢ المرجع والمعيار

```
المرجع الأساس:  main @ 3d29066  (4-8-2026)
شرط القبول:    كل سجل موجود يبقى بقيمه حرفيًا · والزيادة سجلات جديدة فقط
الأولوية:      المُخرَج المطابق يقدّم على البنية دائمًا
               وأي تنظيف يهدّد حكمًا يُلغى — لا يُخفَّف ولا يُؤجَّل
```

### الفرق المسموح — خمسة بنود مُعدَّدة. لا سادس بلا قرار.

| # | البند | النوع | الحالة |
|---|---|---|---|
| 1 | `AuditRunResult` — سبعة حقول | تصحيح | ✅ |
| 2 | دلالة الحجب في V2 | تصحيح | ✅ |
| 3 | عطل `run_audit_compat` + فضاء المعرّفات | تصحيح | ✅ |
| 4 | نسبة الامتثال بعد الخطوة ٤ | فرق عرض | ⬜ بخطّ فاصل |
| 5 | **مسار المستندات لا يُنتج نتيجة** | تصحيح | ⬜ المرحلة ٢ |

## ١.٣ قواعد التشغيل — مستخلَصة من العمل لا من كتاب

1. **الوثيقة لا تفشل، الاختبار يفشل.** كل مرحلة تنتهي بحارس لا بعلامة.
2. **حارس لم يُر وهو يفشل ليس حارسًا** — ويُثبَت بإعادة إنتاج العيب، لا
   بمخالف مصطنع. (ثلاثة حرّاس وُلدوا عاجزين في هذا المستودع.)
3. **الرقم بلا مسار ليس رقمًا.** ولا قائمة يدوية لما يمكن حسابه.
4. **حين تتعارض الشيفرة مع قياس، الشيفرة أقرب** حتى تُثبت الأداة صحّتها.
   **خمس أدوات قياس أعطت أرقامًا واثقة عن سؤال آخر.**
5. **أنهِ أو احذف. لا تترك اثنين.**
6. **`main` جامد.** لا دمج ولا نشر إلا بقرار صريح.
7. **مرحلة واحدة في وقت واحد. لا تواريخ.**
8. **بعد كل مرحلة: يوم بلا عمل عليها.** أكثر أخطاء هذه السلسلة وقعت في
   اللحظة التي تلت إنجازًا.

---

# الجزء الثاني — الواقع المقيس

## ٢.١ خمس منظومات حكم · 191 قاعدة

| المنظومة | القواعد | تعمل على |
|---|---|---|
| المدقق القديم | 34 | الفواتير — متزامن |
| المحرك النشط V1 | 131 | الفواتير · API · GAAP |
| المحرك النشط V2 | 131 | 11 نوعًا مُوقَّعًا |
| تحليلات القيود | 8 | دفتر الأستاذ |
| `AuditEngine` القديم | 18 | **مسار المستندات — ولا يوصل شيئًا** |

**والرقم المعلن في `README.md`: 30.**

## ٢.٢ التقييم على المحاور الستّة

| المحور | التقدير | الدليل |
|---|---|---|
| الفساد والتلاعب | 🟢 9/10 | قيد تفرّد ⇒ التفرّع مستحيل بالبناء · `ChainCheckpoint` |
| **تعدد المستأجرين** | 🟢 **9/10** | صفر فجوة · مسار الكتابة مُغلَق · 192 نموذجًا مُصنَّفًا |
| **الأمن** | 🟢 **9/10** | Django 5.2 LTS · ترويسات كاملة · MFA · SSRF |
| الأعطال | 🟡 7/10 | ثلاث درجات AI · قاطع دائرة · `on_commit` |
| SOLID | 🟡 5/10 | `RuleRegistry` سليم · مقابل 5 منظومات و`page_views` 5,869 |
| **الحمل** | 🔴 **لا تقدير** | اختبار الحمل يقرأ ست صفحات بـGET |
| **الواجهة** | 🔴 **غير مفحوصة** | صفر فتح متصفّح منذ ترقية الإطار |

## ٢.٣ خمسة أعطال — ثلاثة مُصلَحة

| # | العطل | الحالة |
|---|---|---|
| 1 | `AuditRunResult` ينقصه 7 حقول ⇒ فشل كاذب لكل مستند ناجح | ✅ |
| 2 | `run_audit_compat` — `RecursionError` | ✅ |
| 3 | فضاء المعرّفات — `Document.DoesNotExist` على كل رفع | ✅ |
| 4 | **فواتير المبيعات لا تستهلك حصّة** — أثر تجاري | ⬜ قرار مشغّل |
| 5 | **مسار المستندات لا يُنتج نتيجة** — `passed_count` مقابل `passed_rules` | ⬜ المرحلة ٢ |

**كلها كانت قائمة قبل هذه المحادثة. لم نصنعها — كشفناها.**

## ٢.٤ ما أُنجز

**12 حارسًا دائمًا** · **~70 انحرافًا مسجَّلًا، ثلثها أخطاء المشرف** ·
**مرجع ذهبي مُثبَت** (`dd46332f…` مطابق عبر ست شحنات) ·
**18 commit على `claude`، غير مدموجة.**

---

# الجزء الثالث — المراحل

## 🔴 المرحلة ١ — فحص الواجهة · **تسبق كل شيء**

**السبب:** ترقية Django تمسّ التصيير والقوالب والمسارات، و3,995 اختبارًا
خضراء **لا تُثبت أن صفحة تُعرض**. وأربعة تقارير قالت «لم أفحصها».

**خطئي:** سجّلت التحذير ولم أجعله بوابة. **والتسجيل ليس حماية.**

```bash
python manage.py collectstatic --noinput      # لا --dry-run
python -c "from django.conf import settings; print(settings.DEBUG)"
DEBUG=False python manage.py runserver
```

⚠️ `DJANGO_DEBUG=False` **لا يعمل** — الاسم `DEBUG`. اطبعه ولا تفترضه.

**ستّ صفحات:** `/dashboard/` · `/invoices/` · `/dashboard/files/` ·
`/platform-admin/` · تقرير تدقيق · صفحة Alpine.

**والحاسم:** أن **يتفاعل** عنصر Alpine — لا أن يوجد. و`console` بلا خطأ.
الصفحة ترجع `200` وهي جامدة، وهذا ما لا يكشفه أي اختبار (`8c2f9cb`).

⏸ **نقطة توقّف:** تعرف حالة الواجهة يقينًا. **وخوف «المفاجأة عند النهاية»
ينتهي هنا لا في النهاية.**

---

## المرحلة ٢ — أعِد مسار المستندات إلى العمل

```
core/services/pipeline.py:435   يقرأ   passed_count · failed_count · …
apps/audit/audit_engine.py:134  يسمّي  passed_rules · failed_rules · …
⇒ AttributeError على كل مستند · يبتلعه except Exception
⇒ DocumentAnalysisResult: صفر صفّ
```

**الإصلاح: أربع `@property` أسماء بديلة.** لا إعادة تسمية — مستدعون آخرون
يقرأون الأسماء القائمة.

**والحارس:** اختبار يستخرج الحقول الأحد عشر **بالـAST** من
`_serialise_audit_report` ويؤكّد أن `AuditReport` يعرضها كلها. **قائمة يدوية
ثالثة ستنحرف كما انحرفت الأوليان.**

⏸ **نقطة توقّف:** المسار يُنتج نتيجة. **وهذا خطّ الأساس الذي بدونه الخطوة ٤
بلا معنى.**

---

## المرحلة ٣ — اعرف سقفك

**العائق:** أداة الحمل **تقرأ ست صفحات بـ`GET`** — صفر رفع، صفر تدقيق. ووثيقتها
تقول إنها «تقود الأنبوب». **رابعة أداة تدّعي ما لا تفعله.**

**فالمرحلة بندان:** اتفاق مكتوب على ما تقيسه الأداة · ثم بناؤها **مع اختبار
يُثبت أنها تقيس ما تدّعيه.**

**الأعناق المرشّحة (من الشيفرة، غير مُقاسة):** المعالجة متزامنة على مسار
الفواتير · ~12 استعلامًا لكل فاتورة · كل مستند يُدقَّق مرتين · 645 استعلامًا
في 70 وحدة عرض · MySQL واحد بلا نسخ قراءة.

**التقدير المعماري:** عشرات ✅ · مئات 🟡 · آلاف 🔴

⏸ **نقطة توقّف:** رقم سعة منشور بأعناقه. تحتاج خادمًا.

---

## المرحلة ٤ — منظومة تسقط

بالترتيب، وكل بند شحنة منفصلة:

| # | العمل | الأثر |
|---|---|---|
| 1 | حاجب `invoice_id` في الجسر | قرار معماري · يفتح ٢ |
| 2 | الخطوة ٤ — مسار المستندات | **صفر → 131** قاعدة |
| 3 | الخطوة ٦ب — تحويل مستدعي V1 الأربعة | انشقاق V1/V2 يسقط |
| 4 | حذف `AuditEngine` | 18 قاعدة · **5 → 4** |
| 5 | حسم `CTL-004`/`CTL-04` · `CTL-005`/`CTL-05` | مفتاح هوية غامض |

🔴 **بوابة البند ٢: صفر انقلاب `blocks_approval` من False إلى True.**
مستند كان يُعتمَد ولا يُعتمَد بعدها = رفض. **والمتوسطات لا تكشفه.**

⚠️ **والمرجع الذهبي لا يحرس هذه المرحلة** — المولّد يتجاوز التوجيه. الحارس
مقارنة مباشرة بين المحرّكين.

⏸ **نقطة توقّف:** منظومات الحكم 5 → 3. **أول رقم بنيوي يتحرّك.**

---

## المرحلة ٥ — برهان أن النمط يُعمَّم

**لا تُفكّك 191 قاعدة. فكّك خمسًا وأثبت الطريق.**

مجموعة `DUP` — الخمسة تتقاسم الوقائع. و`AuditContext` **موجود ومُجرَّب** في
`journal_analytics.py`: سجل + دوال نقية + سياق واحد.

قاعدة واحدة = commit واحد = تشغيل المرجع الذهبي.
ومعها: توصيل `providers.py` بمواقعه السردية الثلاثة.

⏸ **نقطة توقّف:** الطريق مُبرهَن بخمس قواعد لا موصوف بخطة.

---

## المرحلة ٦ — العرض

**ليست خاتمة — إنها المرحلة التي يُقرأ فيها كل ما سبق.**

| # | العمل |
|---|---|
| 1 | **`ARCHITECTURE.md`** — القرارات وأسبابها · الدَّين بالأرقام · آليات منع التراجع |
| 2 | **`README.md` صادق** — 191 قاعدة · MySQL · العتبات الحقيقية |
| 3 | رقم السعة وأعناقه |
| 4 | `EXECUTION_TRACKER` §15 معروضًا — **70 انحرافًا، ثلثها أخطاء المشرف** |
| 5 | `git mv Docs Documentation` — 13 مرجعًا نصّيًا · Linux حصرًا |

**البند ٤ أقوى ورقة تملكها.** مراجع خبير لا يعاقب على الدَّين — يعاقب على
الدَّين **غير المعروف**.

---

# الجزء الرابع — البنود المفتوحة

## ٤.١ تحتاج قرارك — لا كودًا

| البند | السؤال |
|---|---|
| **فجوة حصّة الفواتير** | فواتير المبيعات لا تستهلك حصّة اليوم. الفوترة الرجعية؟ |
| **الـ1003 صفًّا بلا مالك** | تُحذف · تُنسب · أم تبقى شاهدًا؟ الجواب يفتح `NOT NULL` |
| **دمج الـPR** | 18 commit · ثلاثة إصلاحات عطل تنتظر · دقيقتان |
| **بيئة عرض/خادم** | تفتح: فحص الواجهة · قياس السعة · ملف القفل |

## ٤.٢ تحتاج بيئة

`requirements.lock.txt` (10 حزم غائبة منها `pyotp`) · بيان whitenoise ·
قياس السعة · فحص الواجهة.

## ٤.٣ مؤجَّل بقرار — يُكتب في `ARCHITECTURE.md` ولا يُخفى

`page_views.py` (5,869 سطرًا) · الـ191 قاعدة كلها · ربط
`OrganizationScopedManager` بالـ111 نموذجًا · تصفية موضع القراءة (محجوبة على
تصفير الـ`NULL`) · CSP `unsafe-eval` · كتاب القواعد · برنامج البيانات ·
إعادة تدقيق مستندات ما قبل 4 أغسطس.

**دَين مكتوب بخطة يُقرأ إدارةً. دَين مخفي يُقرأ فقدان سيطرة.**

---

# الجزء الخامس — النشر

## ٥.١ محجوب على ثلاثة

```
☐ فحص متصفّح — FRONTEND_CONTRACT §5
☐ بيان whitenoise مبنيًّا فعليًا (لا --dry-run)
☐ يومان مراقبة على dev
```

## ٥.٢ التراجع

`main` على `3d29066` لم يُمسّ. و18 commit كلها على `claude`.

⚠️ **الرجوع عن Django 5.2 ليس `pip install Django==4.2.*` وحده** —
`condition=` تحتاج عكسًا يدويًا (أُدخل في 5.1).

## ٥.٣ ما يجب إعلانه قبل النشر لا بعده

- المرحلة ٢: مستندات لم تكن تُنتج نتائج ستُنتجها ⇒ **عدّادات تقفز**
- المرحلة ٤: نسبة الامتثال تتحرّك بلا تغيّر جودة مستند
- وربما مستندات تتحرّر من حجب سابق

**مدقق يرى أرقامه تتغيّر بلا إشعار يفقد الثقة في المنصّة — وهذا أشدّ أثرًا من
أي عيب تقني أصلحناه.**

---

# الجزء السادس — مقاييس النجاح

| المحور | اليوم | الهدف |
|---|---|---|
| الواجهة | 🔴 غير مفحوصة | 🟢 مفحوصة |
| مسار المستندات | 🔴 صفر نتيجة | 🟢 يعمل |
| السعة | 🔴 غير مقيسة | رقم منشور |
| الأمن | 🟢 9/10 | 🟢 10/10 (القفل) |
| المستأجرون | 🟢 9/10 | 🟢 9/10 |
| منظومات الحكم | 5 | **3** |
| مخططات الترجيح | 4 | 3 |
| المعمارية موثَّقة | ❌ | ✅ |

**التقدّم: ٣٦%** — والمقياس هو المخرَج البنيوي، لا النشاط.

---

# الجزء السابع — كلمة أخيرة

**لا يوجد «عندما نخلص».** `main` لم يُمسّ، و18 commit على فرع منفصل، ولا شيء
يصل عميلًا إلا بقرارك. فليست هناك لحظة مفاجأة كبرى — **هناك نشرة واحدة، تحت
سيطرتك، ونصف ساعة فحص تسبقها.**

**والمرحلة ١ هي تلك النصف ساعة.** ابدأ بها، وينتهي الخوف اليوم.

وإن توقّفت عند المرحلة ٢: **ثلاثة أعطال مُصلَحة · محوران أخضران · مسار
المستندات يعمل · 12 حارسًا · ومرجع يُثبت أن الأحكام لم تتغيّر.**

**هذا أكثر مما يملكه أغلب ما يُعرض على مراجعة تقنية. والباقي تحسين، لا إنقاذ.**
