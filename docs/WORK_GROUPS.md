# مجموعات العمل — الحالة والمتبقّي

> يُحدَّث عند إغلاق أي بند. كل رقم هنا **مقيس**، وأمر قياسه مذكور بجانبه.
> آخر قياس: 2026‑08‑19

---

## الحالة الآن

| | |
|---|---|
| الإنتاج `tadgeeg.com` | `fe47b41` · 8 هجرات مطبَّقة · HTTP 200 |
| `origin/main` | `ce38d7d` — يسبق الإنتاج بسكربتين، **لا هجرات بينهما** |
| `test.tadgeeg.com` | 🔴 معطوب — تاريخ هجرات غير متّسق |
| الانحدار | 4240 مجموعة · آخر تشغيل كامل 4228 ✓ / 0 ✗ |
| النسخة الاحتياطية | `deployment/docker/backups/live-20260819-184446` · ونسخة خارج الخادم |

```bash
git rev-parse --short origin/main
pytest -q --collect-only 2>&1 | tail -1
```

---

## المجموعة أ — سلسلة التدقيق (§1 · الأعلى أولوية)

**الحالة:** 17 سلسلة على الإنتاج · **16 سليمة · 1 مكسورة**

| البند | الحالة |
|---|---|
| السبب مشخَّص | ✅ `0010` جزّأت `str(user_id)` بشرطات · `0011` ملأت `chain_actor` بـ`F("user_id")` بلا شرطات |
| `chain_actor` لا يقرؤه غير الهاش | ✅ مقيس — 3 مواضع، كلّها حمولات تجزئة |
| أمر التصحيح | ✅ `manage.py repair_chain_actor` · فحص افتراضيًّا · يطبع عكس كل كتابة |
| الحرّاس | ✅ 7 · تزرع مسار الهجرة الذي أفلت من `test_audit_trail_integrity` |
| 🔴 **تشخيص صفّ الإنتاج** | **لم يُقَس** — استُنتج من التشابه. القياس هو §أ‑1 أدناه |
| التطبيق على الإنتاج | ⏸ بعد القياس |

**§أ‑1 — القياس الناقص، قراءة فقط:**
```bash
docker compose -f deployment/docker/docker-compose.yml run --rm -T \
  --entrypoint sh web_live -c 'python -' < <(sed 's|^|  |;1d' /dev/null)  # انظر §أ‑2
```

**§أ‑2 —** نفس العيب كامن في `activity_logs/0006:24` · الجدولان فارغان على الإنتاج.

---

## المجموعة ب — مسار النشر

| البند | الحالة |
|---|---|
| `deploy_live.sh` | ✅ 8 مراحل · بوابة خادم · فحص جافّ · هجرة في حاوية `--rm` |
| حارسه | ✅ 10 اختبارات |
| خطة الهجرات تُقرأ من الصورة الجديدة | ✅ `be1c60b` |
| 🔴 `test.tadgeeg.com` | **يُصلَح بسحب `main` هناك** — لا SQL يدوي |
| توحيد `update.sh` | ⏸ ينقصه: بوابة الخادم · فحص جافّ · الهجرة نصف‑المطبَّقة · الشقوق · مفاتيح البريد · ورفع قيد `live\|all` |

---

## المجموعة ج — PostgreSQL

**القياس تمّ. القرار لم يُتَّخذ.**

| القياس | النتيجة |
|---|---|
| SQL خام في المستودع | **موضع واحد** — `cursor.execute("SELECT 1")` |
| دوال MySQL خاصّة | **لا شيء** |
| قيود شرطية لا يبنيها MySQL | **12** · **صفر مخالفة على الإنتاج** ✅ |
| سلاسل الهاش | 16/17 — تُحسم في المجموعة أ أوّلًا |
| نماذج · JSONField | 208 · 315 |

**المكسب:** DDL معامِلاتي (يمنع حلقات الإقلاع التي كلّفتنا يومين) · و12 قيدًا تعمل بلا حراسة اليوم.
**الباقي قبل القرار:** التسلسلات (`sequences`) · حسّاسية حالة الأحرف (تُرخي قيد البريد).

---

## المجموعة د — عدّاد الذكاء الاصطناعي

| | |
|---|---|
| البوابة `core/ai/gateway.py` | ✅ موجودة · 5 مستدعين · ميزانية وكلفة وتصنيف فشل |
| النماذج والهجرة | ✅ `ai_safety.0002` · مطبَّقة على الإنتاج |
| 🔴 نداءات خارج البوابة | **7 ملفات** · أربعة منها **لا تعرف المنظّمة إطلاقًا** |
| الكلفة الحقيقية | تمرير مستأجر عبر **22 موضع استدعاء** — لا خمسة ملفات |
| حاجب إداري | مفتاح OpenAI يردّ `401` — تدويره ليس عملًا برمجيًّا |

```bash
grep -rl 'chat\.completions\.create' --include=*.py apps/ core/ | grep -vc gateway.py
```

---

## المجموعة هـ — حواجز خارجية (لا تُحلّ بكود)

`S3/MinIO` · `Odoo` · `WhatsApp` · البريد الحيّ · حصص API

**لا يوصف أيّها بالجاهزية التشغيلية قبل اختبار حيّ.**

---

## بنود مسجَّلة، غير مفتوحة

- `apps/rule_engine/catalog.py` محجوب **بقرار** — `docs/CATALOG_SHADOW_IMPACT.md`
- 402 مقابل 403: أي طبقة ترفض أوّلًا لمستخدم بلا منظّمة
- الباقات المحاسبية الثلاث: تُخفى بعد فحص الاشتراكات النشطة
- `9c02c2c` وُصف «وثائق فقط» وحمل نقل ملف — مدفوع، مُصرَّح به في `a20bf7c`
