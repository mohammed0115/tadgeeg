# الافتراض يتوقّف عن الادّعاء — قياس · 2026-08-12 · `claude @ a5843e1`

> نفس الـ34 · نفس السكربت حرفيًّا قبل وبعد. قراءة-فقط: `Document` 1853→1853 ·
> `AuditRun` 3→3.

---

## 🔴 الخلاصة أولًا

**الافتراض توقّف عن الادّعاء، ولم يتوقّف عن الوجود.**

| | قبل | بعد |
|---|---|---|
| مستندات بثقة **0.90** على قيمة لم يُحدّدها أحد | **17** | **0** ✅ |
| مستندات صُنّفت `invoice` | 17 | **17** ⚠️ |
| القواعد الفاشلة · الدرجات · التصعيد | — | **متطابقة تمامًا** |

التخمين **انتقل ولم يزُل**: كان يخرج من الفرع البنيوي بثقة 0.90، وصار يخرج
من فرع الكلمات بثقة **0.667**.

```
method قبل : {structural: 17, default: 14, keyword: 3}
method بعد : {keyword: 20, default: 14}        ← صفر structural
```

والسبب في `classify` نفسها، **خارج النطاق المصرَّح به لهذه الشحنة**:

```python
keyword_result = self._classify_keywords(...)
if keyword_result and keyword_result["confidence"] >= 0.70:
    return keyword_result          # 0.667 يسقط هنا — رُفض صراحةً

if use_ai and raw_text: ...        # يُتخطّى حين use_ai=False

if keyword_result:
    return keyword_result          # 🔴 ويُعاد رغم رفضه، بلا حدّ أدنى
```

**العتبة تُفحَص ثم تُهمَل.** الخطوة 4 تُعيد ما رفضته الخطوة 2.

---

## ما تغيّر فعلًا — وما لا يقيسه جهاز التطوير

قياسي كلّه بـ`use_ai=False` (حتمي · بلا كلفة). وهذا يُخفي **الأثر الحقيقي**:

| | قبل الإصلاح | بعده |
|---|---|---|
| الفرع البنيوي | يُعيد 0.90 ⇒ **يُغلق الطريق** | يسقط |
| فرع الكلمات | لا يُبلَغ | 0.667 — **دون العتبة** |
| فرع الذكاء الاصطناعي | **لا يُستدعى أبدًا** | **يُستدعى** (في الإنتاج) |

⇒ في الإنتاج (`use_ai=True`) صار المُصنِّف الذكي يُستشار على هذه الـ17
لأول مرة. **وهذا لم يُقَس** — قياسه يستهلك OpenAI على 34 مستندًا، ولم
أُنفقه بلا تصريح.

---

## §١ — من يقرأ `document_type`

مسح بالـ`ast` (لا `grep`: يلتقط النصّ في تعليق ولا يميّز قراءة من إسناد):

```
109 موضع قراءة في 56 ملفًا
  49  apps/rule_engine     24  core/services      11  apps/documents
   9  apps/reports          4  apps/auditing       4  apps/invoices
```

**الافتراضات المُمرَّرة إلى `.get("document_type", X)`:**

| الافتراض | العدد | الموضع |
|---|---|---|
| `""` | 6 | `pipeline.py:292` · `document_classifier.py:139` · … |
| `"other"` | 4 | `document_engine.py:134` · `financial_ai_engine.py:301` · … |
| **`"invoice"`** | **1** | **`normalization.py:145`** — وحده في المستودع كلّه |

**ولا مستهلك واحد يقارن بـ`"unknown"`** — القيم المقارَنة هي
`sales_invoice` ×8 · `invoice` ×5 · `purchase_order` ×4 · `sales_receipt` ×4 ·
`bank_statement` ×3 · `vat_return` ×3 · `payroll` ×2 · `grn` · `tax_return`.
قيمة لا تطابق أيًّا منها تسقط من فروعهم — كما يفعل `"other"` تمامًا.

### 🔴 موضع تخمين ثانٍ — فات الماسح، وخارج نطاقي

```
core/services/compliance/vat_validator.py:103
  document_type = str(document.get("document_type") or "invoice").strip().lower()
```

صيغة `or` لا يلتقطها ماسح `.get(key, default)`. **أكملتُ المسح بثانٍ فوجدته.**
خارج النطاق المصرَّح به ⇒ **مُبلَّغ، غير مُصلَح.**

### `"unknown"` مقبول؟

**ليس خيارًا في `Document.DocumentType`** (العشرة: `invoice` · `receipt` ·
`bank_statement` · `ledger` · `contract` · `purchase_order` ·
`expense_report` · `tax_document` · `financial_statement` · `other`).

**ولا يصل العمود:** `_persist_result` يمرّره عبر
`type_map.get(ai_type, "other")` ⇒ يُخزَّن `"other"`، وهو خيار صالح.
و`get_document_type_display()` على قيمة غير مخرَّطة يُعيد النصّ نفسه — لا
فراغًا ولا خطأ (§٧).

---

## §٤ — الأرقام السبعة

| # | | |
|---|---|---|
| ١ | تغيّر نوعها | **0 / 34** |
| ٢ | صارت `unknown`/`other` | **0** |
| ٣ | تحرّرت من التصعيد إضافةً | **0 / 18** |
| ٤ | صعّدت ولم تكن | **0** ✅ |
| ٥ | فقدت قاعدة تنطبق عليها | **0** ✅ — لا مستند تغيّر نوعه، فلا قاعدة فُقدت |
| ٦ | أخطاء | **0** ✅ |
| ٧ | **ثقة 0.90 على قيمة مفترَضة** | **17 → 0** ✅ |

الرقم ٧ هو مخرَج هذه الشحنة الوحيد المقيس. والأرقام ١–٣ أصفار **لأن العيب
المتبقّي في موضع لم يُصرَّح لي بلمسه**.

---

## ما نُفِّذ

**أ. `normalization.py:145`** — `"invoice"` ⇒ `"unknown"`، أي ما يقوله
جاراه في السطر نفسه (`extraction_method` و`language`).

**ب. `document_classifier.py`** — الفرع البنيوي يرفض السنتينلات
(`_UNDETERMINED = {"unknown", "", None}`) بدل أن يختمها 0.90. و`"unknown"`
خارج `DOCUMENT_TYPES` أصلًا فكان سيسقط — والتسمية الصريحة تمنع إعادة فتح
الثغرة لو أُضيف إليها لاحقًا.

**ج. `pipeline.py` — `type_map`** — لم تُوسَّع (المُصنِّف لا يُرجع
`ledger`/`contract`/`financial_statement`، وتوسيعها اختراع مسار). أُضيف
**تسجيل** لكل قيمة تسقط إلى `"other"`: `"unknown"` هي الحالة الطبيعية،
لكن قيمة جديدة من مُصنِّف يتّسع هي **نوع مستند لا يُدقَّق** — والتسجيل
يجعلها مرئية. **تسجيل لا استثناء**: قيمة جديدة يجب ألّا تُوقف رفعًا.

**`payroll` → `other`** لم يُصلَح: لا خيار `payroll` في `DocumentType`،
وإضافته migration خارج النطاق. مُعلَّق بتعليق في الشيفرة وبالانحراف 74.

---

## ما يحتاج قرارًا

1. **الخطوة 4 في `classify`** — تُعيد ما رفضته العتبة. هذا هو ما تبقّى من
   العيب، وإصلاحه (حدّ أدنى، أو السقوط إلى `"unknown"`) قرارك.
2. **`vat_validator.py:103`** — موضع تخمين ثانٍ لـ`"invoice"`.
3. **قياس بـ`use_ai=True`** — الأثر الحقيقي للإصلاح غير مقيس، وقياسه
   يستهلك OpenAI.
