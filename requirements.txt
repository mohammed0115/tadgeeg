Django==5.2.*
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
drf-spectacular>=0.27
django-filter>=23.5
django-countries>=7.6          # ISO 3166-1 country data + Arabic labels for lead capture
channels>=4.2.0
channels-redis>=4.2.0
celery>=5.3
redis>=5.0
sentry-sdk>=1.45          # Error tracking and performance monitoring
openai>=1.35
pytesseract>=0.3.10
pillow>=10.4.0
qrcode>=8.0
weasyprint>=69.0
google-auth>=2.35.0
requests>=2.33.0        # PYSEC-2026-2275
PyMuPDF>=1.23           # PDF to image conversion
psycopg2-binary>=2.9    # Optional PostgreSQL adapter for non-SQLite deployments
scikit-learn>=1.3       # Statistical ML models
numpy>=1.25
pandas>=2.0
# scipy was pinned in requirements.lock.txt but never listed here, and the
# Dockerfile installs THIS file — so apps/analytics/benford_service.py, whose
# `from scipy import stats` is a top-level import, could not be imported in any
# built image. See tests/test_declared_dependencies.py for the guard.
scipy>=1.11             # chi-square goodness-of-fit for Benford analysis

# Imported at module level and previously undeclared — each one worked only
# because some *other* package happened to pull it in. That holds until an
# unrelated upgrade drops the transitive edge, at which point the failure is a
# boot-time ImportError with no obvious cause.
cryptography>=48.0.1    # Fernet for payment/banking/ZATCA secrets at rest.
                        # apps/payments/models.py AND a migration import it, so
                        # its absence stops the app from starting at all.
asgiref>=3.7            # core/websocket.py — ships with Django today, but this
                        # code imports it directly rather than through Django.
chardet>=5.2            # core/services/structured_parsers.py — encoding
                        # detection for uploaded CSV/TXT.
python-dotenv>=1.0
mysqlclient>=2.2
gunicorn>=21.0
whitenoise>=6.6         # Static file serving
openpyxl>=3.1           # Excel (.xlsx) export
xlrd>=2.0               # Excel (.xls) legacy format support
pyotp>=2.9              # TOTP / MFA support
django-storages[s3]>=1.14  # S3 / cloud media storage (NFR-2)
boto3>=1.34             # AWS SDK required by django-storages S3
pyzbar  