# بوّابة النوع المركزية — قياس · 2026-08-12 · `claude @ 8d1b0b8`

> العيّنة: نفس الـ34 · نفس السكربت حرفيًّا قبل وبعد، فأي فرق بين الملفَّين
> فرق في المحرّك لا في القياس. قراءة-فقط: `Document` 1853→1853 ·
> `DocumentAnalysisResult` 0→0 · `AuditRun` 3→3.

---

## العيب

```
apps/audit/audit_engine.py   for rule_class in self.rule_classes:
                                 result = rule.evaluate(...)
grep is_applicable audit_engine.py   →  فارغ
```

`AuditRule.is_applicable` موجودة في `base_rule.py:158` منذ كُتبت القواعد،
**والحلقة لم تستدعِها إطلاقًا**. أربع قواعد تستدعيها على أنفسها وأربع عشرة
لا — فالبوّابة كانت **عُرفًا لا آليّة**.

⇒ **العيب ليس أن القواعد نسيت، بل أن `evaluate` لا تسأل.** والإصلاح في
موضع واحد: سؤال واحد قبل النداء.

وغير المنطبقة **تُسجَّل ولا تُهمَل**: `_skip(reason)` يكتب السبب في
`explanation`، و`_build_report` يعدّها في `total_rules` وفي **لا** `passed`
ولا `failed`. قاعدة تختفي بلا أثر تُعطي المدقق ثقة زائفة — لا يعرف أنها لم
تُطبَّق. وهذا هو الفصل الذي يقيمه `apps/audit_platform/status.py` بين «لا شيء
لنجده» و«لم نستطع النظر».

**العدّادات لم تُمسّ** — لم أحتج: `total_rules = len(results)` أصلًا،
و`_skip` سبيل قائم تستعمله أربع قواعد منذ البداية.

---

## النطاقات — قرار كل قاعدة وسببه

القرار من **منطق القاعدة** لا من اسمها. ومفردات النوع التي يراها المحرّك هي
الثمانية التي يُرجعها المُصنِّف (`invoice` · `purchase_order` ·
`bank_statement` · `receipt` · `expense_report` · `payroll` · `vat_return` ·
`other`) — لأن `type_map` يُطبَّق في `_persist_result` **بعد** المحرّك.

### أُعلن نطاقها (7)

| القاعدة | النطاق | السبب |
|---|---|---|
| **R007** المطابقة الثلاثية | `invoice` | المطابقة تُجرى **على** الفاتورة مقابل أمر الشراء وسند الاستلام. لا شيء آخر في المفردات هو الضلع الثالث. |
| **R010** الأرقام المستديرة | `invoice` · `receipt` · `expense_report` | فرضيتها المكتوبة «الفواتير الحقيقية عادةً بكسور». وهي كاذبة لغيرها: عقد بـ50,000 أو راتب بـ120,000 مستدير لأنه كذلك، لا لأن أحدًا كتبه بيده. |
| **R012** المبالغ السالبة | `invoice` · `purchase_order` · `receipt` · `expense_report` | وصفها نفسه: «إشعارات الدائن نوع مستند منفصل». والمثال المضادّ كشف الحساب البنكي — المدين سالب بالتعريف، ووسمه وسم للحساب. |
| **R013** صلاحية تاريخ الاستحقاق | `invoice` · `purchase_order` | «Net 365» لغة شروط سداد. تاريخ استحقاق على كشف بنكي أو إقرار ضريبي مفهوم آخر. |
| **R015** اعتماد المورّد | `invoice` · `purchase_order` · `receipt` · `expense_report` | نفس النطاق الذي يُعلنه `R006` أصلًا وللسبب نفسه: الأنواع التي يكون فيها الطرف المقابل مورّدًا يُدفع له. |
| **R017** رمز ZATCA | `invoice` · `receipt` | المرحلة الثانية تُلزم الفاتورة الضريبية والفاتورة الضريبية المبسَّطة — وهي `receipt` هنا. ولا تُلزم عقدًا ولا كشف حساب. |
| **R018** الحقول الإلزامية | `invoice` · `receipt` | `REQUIRED_ALIASES` قائمة حقول فاتورة. طلبها من كشف عميل هو مطالبته بأن يكون فاتورة. نفس نطاق R017 لأن كليهما من تعريف ZATCA للفاتورة الضريبية. |

**منطق `R017` في SAR/SA لم يُمسّ** — البوّابة بالنوع وحدها.

### عامّة بقصد (7) — والقائمة سقف يحرسه اختبار

`R003` (مُوَعّاة داخليًّا بـ`REQUIRED_FIELDS_BY_TYPE`) · `R005` (تُعلن
`set()` صراحةً) · `R008` (عملة صالحة مطلوبة من أي مستند مالي) · `R009`
(تتخطّى نفسها بلا `vat_rate`) · `R011` (تتخطّى نفسها بلا `line_items`) ·
`R014` (صيغة TRN واحدة أيًّا كان حاملها) · `R016` (خانتان عشريتان حدّ على
المبلغ لا على النوع).

`R003` و`R005` **خارج النطاق المصرَّح به** لهذه الشحنة أصلًا (ملفّاهما ليسا
`extended_rules.py`) — وكلاهما عامّ بحقّ.

**لم أُخمّن نطاقًا واحدًا.** كل قاعدة لم يكن نطاقها بيّنًا من منطقها تُركت
بلا بوّابة — الغياب يُبقي الحال، والنطاق الخاطئ يُسكت قاعدة يجب أن تعمل.

---

## الأرقام الستّة

| # | | |
|---|---|---|
| ١ | تحرّرت من التصعيد | **16 / 34** |
| ٢ | صعّدت ولم تكن | **0** ✅ |
| ٣ | نتائج صارت `SKIPPED` جديدًا | **55**، من 4 قواعد |
| ٤ | قاعدة سُكِّتت على نوع تنطبق عليه | **0** ✅ (فحص يدوي أدناه) |
| ٥ | فارق `risk_score` | متوسط **−22.2** · أدنى −55 · **أقصى 0** — لم يرتفع في مستند واحد |
| ٦ | مستندات أنتجت خطأ | **0** ✅ |

```
تصعيد: 34 → 18     needs_review: 34 → 18
total_rules ثابت 18 لكل مستند
passed + failed + skipped + errors == total_rules  ✅ لكل مستند
```

أمثلة:

```
journal_entry   55/high     ->   0/low       فشل ['R010','R017','R018'] -> []
purchase_order  75/critical ->  25/medium    فشل ['R005','R017','R018'] -> ['R005']
fixed_asset     60/high     ->  35/medium    فشل ['R003','R005','R018'] -> ['R003','R005']
```

### البند ٤ — الفحص اليدوي

القواعد التي سُكِّتت والأنواع المخزَّنة التي سُكِّتت عليها:

| القاعدة | عدد | الأنواع المخزَّنة |
|---|---|---|
| R010 | 10 | contract · goods_receipt_note · journal_entry ×2 · payment_voucher · purchase_order ×2 · quotation · sales_order ×2 |
| R012 | 16 | + customer_statement · fixed_asset ×2 · other ×2 · payroll |
| R017 | 13 | contract · customer_statement · goods_receipt_note · journal_entry ×2 · other ×2 · payment_voucher · purchase_order ×2 · quotation · sales_order ×2 |
| R018 | 16 | كما R012 |

**لا فاتورة واحدة بينها.** والفحص الصريح: صفر مستند نوعه المخزَّن
`purchase_invoice` أو `proforma_invoice` أو `sales_receipt` سُكِّتت عنه قاعدة
فاتورة.

---

## 🔴 قيد يجب أن يُقرأ مع الأرقام

**البوّابة صحيحة بقدر صحّة التصنيف.** وهي تسأل عن `document_type` الذي
يُنتجه المُصنِّف — والمُصنِّف مكسور بقياس موثَّق
(`docs/CLASSIFICATION_MEASUREMENT.md` · الانحراف 71): `normalization.py:145`
يجعل الافتراضي `"invoice"` فيُعاد بثقة 0.90.

الأثر على الاتجاهين:

* **ما سُكِّت عنه**: كله مستندات صُنّفت `other` أو `bank_statement` أو
  `payroll`، وأنواعها المخزَّنة تؤكّد أنها ليست فواتير. لا ضرر في هذه العيّنة.
* **ما بقي يصعّد (18)**: كلها صُنّفت `invoice` أو `receipt` — ومنها
  `tax_declaration` و`supplier_statement` و`payment_voucher`، وهي **ليست
  فواتير**. فالملاحظات الكاذبة عليها لم تزُل، ولن تزول إلّا بإصلاح
  `normalization.py:145`.

⇒ هذه الشحنة أزالت **نصف** الملاحظات الكاذبة. النصف الآخر معلّق على شحنة
التصنيف، وهي الآن على المسار الحرج بعد أن كانت مؤجَّلة.

---

## أثر الواجهة

**16 مستندًا من 34 تخرج من «تحتاج مراجعة»** إلى `completed`. لم أُعدّل عرضًا،
ولم أُشعِر أحدًا — إشعار المدققين قرار المشغّل، وهو قرار له وجه ثانٍ: مستند
كان في قائمة المراجعة يخرج منها، ومن كان يعتمد على تلك القائمة يجب أن يعرف.
